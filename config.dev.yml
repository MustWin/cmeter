version: 0.1

logging:
    level: 'debug'
    formatter: 'text'

mockapi:
    addr: 'localhost:9090'

api:
    addr: 'localhost:9090'

collector:
    tracking_label: 'com.example.org'
    key_label: 'com.example.org'
    rate: 1800

containers: 'embedded'